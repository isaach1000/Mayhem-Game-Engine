define([], function() {
    "use strict";

    // Private class methods/fields
    
    
    /**
     * @exports util/typeChecker
     */
    var module = {
        // Public class methods/fields
        
        isArray: function(object) {
            return object instanceof Array;
        }
    };

    return module; 
});
